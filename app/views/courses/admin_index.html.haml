- model_class = Course
- content_for :header do
  =t '.title', :default => model_class.model_name.human.pluralize
- model_class = Course
- content_for :header do
  =t '.title', :default => model_class.model_name.human.pluralize

- content_for :breadcrumbs do
  #breadcrumb
    = link_to '/' do
      %i.icon-home
      Home
    = link_to courses_url do
      %i.icon-book
      Courses

.widget-box
  .widget-title
    %h5 Courses list
  .widget-content.nopadding
    %table.table.table-striped.table-bordered.data-table.dataTable{:'data-source' => "/admin/courses.json", :id => 'courses'}
      %thead
        %tr
          %th
          %th= model_class.human_attribute_name(:updated_at)
          %th= model_class.human_attribute_name(:title)
          %th= model_class.human_attribute_name(:short)
          %th= model_class.human_attribute_name(:state)
          %th=t '.actions', :default => t("helpers.actions")
      %tbody


= link_to t('.new', :default => t("helpers.links.new")), new_course_path, :class => 'btn btn-primary'
